# API base URL for the TalkBack backend (Vite injects at build time).
# Local dev: default is http://localhost:8081 if unset.
# Production (e.g. Render Static Site): set to your API URL, e.g. https://your-api.onrender.com
VITE_API_BASE_URL=http://localhost:8081
