# release-readiness-core pin — single source of truth.
# Pinned as part of the SCRUM-261 migration (PR gate engine → release-readiness-core).
# Format: key=value pairs; lines starting with '#' and blank lines are ignored.
#
# Consumers:
#   - scripts/release_readiness_core_pin.sh (helper for CI + local scripts)
#   - .github/workflows/release-readiness.yml (shadow + cutover jobs land in SCRUM-265+)
#   - any future tooling that installs the package or invokes the composite actions
#
# Update procedure:
#   1. Bump version= and sha=/ref= together to a single matching tag from
#      psuthar/release-readiness-core.
#   2. Re-run the SCRUM-262 CLI inventory + composite-action existence checks.
#   3. Re-run the SCRUM-263 gap-test harness against the new pin.
version=0.4.0
sha=3bd6902daec4d5145707d86190dd36f09add5fcf
ref=v0.4.0
